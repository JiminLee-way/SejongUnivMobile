<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ splash screen (API 31+). Samsung One UI 같은 OEM은
         windowSplashScreenAnimatedIcon을 무시하고 launcher icon을 강제로
         splash icon area에 fit시킬 수 있다. 우리 launcher icon이 1024×1024
         liquid glass라 안전 영역(66%) 안에 logo가 안 들어가서 좌상단부터
         잘려 보이는 현상이 발생.

         해결: 안전 영역 60%로 padded된 splash_icon.png를 density별로 명시.
         system이 우리 icon을 쓰든 OEM fallback을 쓰든 양쪽 모두 정상 표시. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
